# Production Environment Variables for Frontend
NODE_ENV=production

# Backend URL - PRODUCTION (IP адрес)
REACT_APP_BACKEND_URL=http://185.174.136.113

# Production optimizations
GENERATE_SOURCEMAP=false
INLINE_RUNTIME_CHUNK=false

# Build optimizations
BUILD_PATH=build